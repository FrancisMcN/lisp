name: Lisp CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: build
      run: gcc -v && gcc ./lisp/lisp.c -o l
    - name: test
      run: ./l tests/type_test.lisp
